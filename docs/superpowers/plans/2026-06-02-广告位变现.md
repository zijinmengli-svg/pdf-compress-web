# 可复用展示广告位 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 TinyPDF 接一个"与广告网络解耦、可复制到未来产品"的展示广告位（AdSense），保持 100% 免费、退掉旧奖励门、占位即无占位（不显示就不占空间）、并把 CSP/法务页补到 AdSense 过审要求。

**Architecture:** 一个自包含的前端组件 `public/ad-slot.js`（纯决策逻辑可单测，DOM/AdSense 注入在 init 内）；服务端用 env(`AD_ENABLED/AD_CLIENT/AD_SLOT`) 经 `/api/config` 下发，并在启用时动态放行广告域名的 CSP；占位与降级靠"默认 hidden、仅 filled 才显示"。零 npm 依赖不变。

**Tech Stack:** Node 原生 HTTP、原生 JS、Node 内置 `assert`（单测）、Google AdSense（展示广告）。

参考 spec：`docs/superpowers/specs/2026-06-01-广告位变现-design.md`

---

## File Structure

- **Modify** `server-simple.js` — 新增 `AD_CLIENT_CFG/AD_SLOT_CFG`；`/api/config` 增 `adClient/adSlot`；`setSecurityHeaders` 改为启用广告时动态放行 Google 域名。
- **Create** `public/ad-slot.js` — 可复用展示广告位组件（`shouldInject` 纯逻辑 + `initAdSlot` DOM/AdSense 注入）。
- **Modify** `public/index.html` — 删除奖励门弹窗 + toast；在主卡片后插入广告位容器；加载 `ad-slot.js`；缓存版本号 bump。
- **Modify** `public/app-simple.js` — 删除奖励门 DOM/逻辑/大文件拦截分支 + 未用的 toast；`initConfig` 读 `adClient/adSlot` 并调 `initAdSlot`。
- **Modify** `public/styles.css` — 新增 `.ad-slot` 规则（与卡片同宽、隐藏不占空间）。
- **Modify** `public/terms.html`、`public/privacy.html` — 改写为"免费不限次 + 展示广告"，补 AdSense/Cookie 声明。
- **Create** `test/ads.test.js` — 服务端集成 + `shouldInject` 单测。
- **Modify** `package.json` — `test` 脚本串联两个测试文件。

> **⚠️ 执行顺序 = 任务编号顺序（Task 1→2→3→4→5→6→7）。** 本文档中 Task 4(index.html) 物理上排在 Task 3(app-simple.js) 之前，但**务必按编号执行：先 Task 3(app-simple.js)、后 Task 4(index.html)**。原因：必须先撤掉 app-simple.js 对弹窗元素的引用，再删 index.html 的弹窗；否则中间态 `null.addEventListener` 会让整个前端脚本崩溃。

---

## Task 1: 服务端广告配置 + /api/config 字段 + 动态 CSP

**Files:**
- Modify: `server-simple.js:30`（config）、`:391-400`（CSP）、`:593-599`（/api/config）
- Create: `test/ads.test.js`

- [ ] **Step 1: 写失败测试**

写入 `test/ads.test.js`：

```js
"use strict";
const assert = require("assert");
const http = require("http");
const { spawn } = require("child_process");

const SRV_DIR = "/Users/libin/Desktop/我的工作流集合/PDF压缩工具-最终版";
let passed = 0, failed = 0;
function check(name, cond, detail) { (cond ? passed++ : failed++); console.log(`${cond ? "PASS" : "FAIL"} - ${name}${detail ? " :: " + detail : ""}`); }
const sleep = ms => new Promise(r => setTimeout(r, ms));

function get(port, path) {
  return new Promise((res, rej) => {
    http.get({ host: "127.0.0.1", port, path, headers: { "User-Agent": "Mozilla/5.0 Chrome/120", "Sec-Fetch-Mode": "navigate" } }, (r) => {
      let d = ""; r.on("data", c => d += c); r.on("end", () => res({ status: r.statusCode, headers: r.headers, body: d }));
    }).on("error", rej);
  });
}
async function startServer(port, env) {
  const srv = spawn("node", ["server-simple.js"], { cwd: SRV_DIR, stdio: ["ignore", "ignore", "ignore"], env: { ...process.env, PORT: String(port), HOST: "127.0.0.1", ...env } });
  for (let i = 0; i < 60; i++) { try { if ((await get(port, "/api/config")).status === 200) return srv; } catch {} await sleep(100); }
  throw new Error("server did not start on " + port);
}

(async () => {
  // ── 广告关闭（默认）──
  const s1 = await startServer(3711, { AD_ENABLED: "", AD_CLIENT: "", AD_SLOT: "" });
  const cfg1 = JSON.parse((await get(3711, "/api/config")).body);
  check("disabled: adsEnabled=false", cfg1.adsEnabled === false, JSON.stringify(cfg1));
  check("disabled: adClient empty", cfg1.adClient === "", String(cfg1.adClient));
  check("disabled: adSlot empty", cfg1.adSlot === "", String(cfg1.adSlot));
  const home1 = await get(3711, "/");
  const csp1 = home1.headers["content-security-policy"] || "";
  check("disabled: CSP strict (no googlesyndication)", !csp1.includes("googlesyndication"), csp1.slice(0, 80));
  s1.kill("SIGKILL");

  // ── 广告启用 + 假 ID ──
  const s2 = await startServer(3712, { AD_ENABLED: "true", AD_CLIENT: "ca-pub-test", AD_SLOT: "123456" });
  const cfg2 = JSON.parse((await get(3712, "/api/config")).body);
  check("enabled: adsEnabled=true", cfg2.adsEnabled === true);
  check("enabled: adClient passed", cfg2.adClient === "ca-pub-test", cfg2.adClient);
  check("enabled: adSlot passed", cfg2.adSlot === "123456", cfg2.adSlot);
  const home2 = await get(3712, "/");
  const csp2 = home2.headers["content-security-policy"] || "";
  check("enabled: CSP allows googlesyndication script", csp2.includes("pagead2.googlesyndication.com"), csp2.slice(0, 120));
  s2.kill("SIGKILL");

  console.log(`\nSUMMARY: ${passed}/${passed + failed} passed`);
  process.exit(failed === 0 ? 0 : 1);
})().catch(e => { console.log("HARNESS ERROR: " + (e.stack || e)); process.exit(1); });
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/ads.test.js`
Expected: FAIL —— `adClient empty` 等断言失败（字段尚不存在 → `undefined`）。

- [ ] **Step 3: 加配置常量**

在 `server-simple.js` 第 30 行 `const AD_ENABLED_CFG       = process.env.AD_ENABLED === "true";` 之后新增两行：

```js
const AD_CLIENT_CFG        = process.env.AD_CLIENT || ""; // AdSense 发布商 ID (ca-pub-…)，过审后在 Railway 填
const AD_SLOT_CFG          = process.env.AD_SLOT || "";   // AdSense 广告单元 ID
```

- [ ] **Step 4: /api/config 增字段**

把（约 593-598）：

```js
      largeFileMB: LARGE_FILE_MB,
      adsEnabled:  AD_ENABLED_CFG,
      maxUploadMB: MAX_UPLOAD_MB,
      gsVersion:   GS_VERSION
    });
```

替换为：

```js
      largeFileMB: LARGE_FILE_MB,
      adsEnabled:  AD_ENABLED_CFG,
      adClient:    AD_CLIENT_CFG,
      adSlot:      AD_SLOT_CFG,
      maxUploadMB: MAX_UPLOAD_MB,
      gsVersion:   GS_VERSION
    });
```

- [ ] **Step 5: 动态 CSP**

把 `setSecurityHeaders` 里的 CSP 设置（约 396-399）：

```js
  res.setHeader(
    "Content-Security-Policy",
    "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; object-src 'none'; frame-ancestors 'none'"
  );
```

替换为：

```js
  // 广告启用时放行 Google 广告域名（否则 AdSense JS 被自身 CSP 挡掉）；关闭时维持严格策略。
  const adScript = AD_ENABLED_CFG
    ? " https://pagead2.googlesyndication.com https://*.googlesyndication.com https://*.google.com https://*.googleadservices.com https://adservice.google.com"
    : "";
  const adFrame = AD_ENABLED_CFG
    ? " https://*.googlesyndication.com https://*.google.com https://*.doubleclick.net"
    : "";
  const adImg = AD_ENABLED_CFG ? " https:" : "";
  res.setHeader(
    "Content-Security-Policy",
    `default-src 'self'; script-src 'self'${adScript}; style-src 'self' 'unsafe-inline'; ` +
    `img-src 'self' data:${adImg}; frame-src 'self'${adFrame}; connect-src 'self'${adScript}; ` +
    `object-src 'none'; frame-ancestors 'none'`
  );
```

- [ ] **Step 6: 运行确认通过**

Run: `node test/ads.test.js`
Expected: PASS —— `SUMMARY: 8/8 passed`

- [ ] **Step 7: 提交**

```bash
git add server-simple.js test/ads.test.js
git commit -m "feat(ads): AD_CLIENT/AD_SLOT config, /api/config fields, dynamic ad CSP" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 可复用广告位组件 `public/ad-slot.js`

**Files:**
- Create: `public/ad-slot.js`
- Modify: `test/ads.test.js`（追加 `shouldInject` 单测）

- [ ] **Step 1: 追加失败测试**

在 `test/ads.test.js` 顶部 `const { spawn } = require("child_process");` 之后加：

```js
const adSlot = require("../public/ad-slot.js");
```

并在 `console.log(\`\nSUMMARY...` 之前插入：

```js
  // shouldInject 纯逻辑
  check("shouldInject: all set → true", adSlot.shouldInject({ adsEnabled: true, adClient: "ca-pub-x", adSlot: "1" }) === true);
  check("shouldInject: disabled → false", adSlot.shouldInject({ adsEnabled: false, adClient: "ca-pub-x", adSlot: "1" }) === false);
  check("shouldInject: no client → false", adSlot.shouldInject({ adsEnabled: true, adClient: "", adSlot: "1" }) === false);
  check("shouldInject: no slot → false", adSlot.shouldInject({ adsEnabled: true, adClient: "ca-pub-x", adSlot: "" }) === false);
  check("shouldInject: null → false", adSlot.shouldInject(null) === false);
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/ads.test.js`
Expected: FAIL —— `Cannot find module '../public/ad-slot.js'`

- [ ] **Step 3: 实现组件**

写入 `public/ad-slot.js`：

```js
// 可复用展示广告位：默认隐藏不占空间；仅当配置了广告且真广告成功填充时才显示。
// 复用到别的产品：拷本文件 + 一个 <div id="..." class="ad-slot" hidden> 容器 +
// /api/config 返回 adsEnabled/adClient/adSlot，然后在配置初始化后调 window.initAdSlot(id, cfg)。
(function () {
  "use strict";

  // 纯逻辑：是否应注入广告。未启用/未配置 → false（容器保持隐藏、不占空间）。
  function shouldInject(cfg) {
    return !!(cfg && cfg.adsEnabled && cfg.adClient && cfg.adSlot);
  }

  // 注入 AdSense 并尝试渲染；仅真广告 filled 才显示容器，否则保持隐藏不占空间（含被墙/无库存）。
  function initAdSlot(containerId, cfg) {
    if (typeof document === "undefined") return;
    var el = document.getElementById(containerId);
    if (!el) return;
    if (!shouldInject(cfg)) return; // 未启用/未配置 → 不注入、容器保持 hidden

    var ins = document.createElement("ins");
    ins.className = "adsbygoogle";
    ins.style.display = "block";
    ins.setAttribute("data-ad-client", cfg.adClient);
    ins.setAttribute("data-ad-slot", cfg.adSlot);
    ins.setAttribute("data-ad-format", "auto");
    ins.setAttribute("data-full-width-responsive", "true");
    el.appendChild(ins);

    var loader = document.createElement("script");
    loader.async = true;
    loader.src = "https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=" + encodeURIComponent(cfg.adClient);
    loader.crossOrigin = "anonymous";
    loader.onload = function () { try { (window.adsbygoogle = window.adsbygoogle || []).push({}); } catch (e) {} };
    loader.onerror = function () { /* 脚本被墙/失败 → 容器保持隐藏 */ };
    document.head.appendChild(loader);

    // 轮询填充状态：filled → 显示；unfilled 或 ~3.5s 超时 → 保持隐藏不占空间。
    var tries = 0;
    var timer = setInterval(function () {
      tries++;
      var status = ins.getAttribute("data-ad-status");
      if (status === "filled") { el.hidden = false; clearInterval(timer); }
      else if (status === "unfilled" || tries >= 7) { clearInterval(timer); }
    }, 500);
  }

  if (typeof window !== "undefined") { window.initAdSlot = initAdSlot; window.adSlotShouldInject = shouldInject; }
  if (typeof module !== "undefined" && module.exports) { module.exports = { shouldInject, initAdSlot }; }
})();
```

- [ ] **Step 4: 运行确认通过**

Run: `node test/ads.test.js`
Expected: PASS —— `SUMMARY: 13/13 passed`

- [ ] **Step 5: 提交**

```bash
git add public/ad-slot.js test/ads.test.js
git commit -m "feat(ads): reusable ad-slot component (collapse-when-no-ad, AdSense inject)" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: index.html — 退奖励门弹窗 + 插广告位 + 加载组件（按编号在 Task 3 之后执行）

**Files:**
- Modify: `public/index.html:103-133`（删弹窗+toast）、`:88-90`（插容器）、`:135`（脚本+版本）

- [ ] **Step 1: 删除奖励门弹窗 + toast**

删除 `public/index.html` 中这一整段（约 103-133，含两段注释、`#ad-modal` 整块、`#toast`）：

```html
    <!-- 广告解锁弹窗（由后台 AD_ENABLED 控制是否启用） -->
    <div id="ad-modal" class="modal-overlay">
      <div class="modal-box">
        <button type="button" class="modal-close" id="ad-modal-close">×</button>
        <h2>大文件需观看广告解锁</h2>
        <p style="color:var(--muted);font-size:14px;margin:8px 0 16px">
          此文件较大，观看一段广告即可解锁本次压缩。
        </p>
        <div id="ad-slot-container" style="
          width:300px;height:250px;margin:0 auto 16px;
          background:#f3f4f6;border:1px dashed #d1d5db;border-radius:12px;
          display:flex;align-items:center;justify-content:center;
          color:#9ca3af;font-size:14px;
        ">广告加载中...</div>
        <div style="background:#e5e7eb;border-radius:4px;height:6px;margin-bottom:8px;overflow:hidden">
          <div id="ad-progress-bar" style="height:100%;width:0%;background:#374151;border-radius:4px;transition:width 1s linear"></div>
        </div>
        <p id="ad-countdown-text" style="text-align:center;font-size:13px;color:var(--muted);margin-bottom:16px">
          请观看广告，5 秒后可继续...
        </p>
        <p class="modal-error" id="ad-modal-error"></p>
        <div class="action-row" style="justify-content:center;gap:12px;flex-wrap:nowrap">
          <button type="button" class="btn-secondary" id="ad-tomorrow-btn" style="flex:1">取消</button>
          <button type="button" id="ad-claim-btn" disabled style="flex:2">请观看广告...</button>
        </div>
        <p class="modal-tip" style="margin-top:8px">小文件始终免费、不限次数</p>
      </div>
    </div>

    <!-- Toast 提示（无广告模式降级用） -->
    <div id="toast" class="toast" hidden></div>
```

（删除后，紧接 `</main>`（约 91）后面、`<footer ...>` 前面这段空白即可，无需保留任何残留。）

- [ ] **Step 2: 插入广告位容器（卡片下方、与卡片同宽）**

把（约 88-91，`</section>` 关闭 `.panel` 后到 `</main>`）：

```html
      </section>
    </main>
```

替换为：

```html
      </section>

      <!-- 展示广告位：默认隐藏不占空间，仅真广告填充时显示；与主卡片同宽、永远在卡片栈最底 -->
      <div id="ad-slot-main" class="ad-slot" hidden></div>
    </main>
```

- [ ] **Step 3: 加载 ad-slot.js 并 bump 缓存版本**

把（约 135）：

```html
    <script src="/app-simple.js?v=1.2.3" defer></script>
```

替换为：

```html
    <script src="/ad-slot.js?v=1.3.0" defer></script>
    <script src="/app-simple.js?v=1.3.0" defer></script>
```

- [ ] **Step 4: 语法/结构自检**

Run: `node -e "const s=require('fs').readFileSync('public/index.html','utf8'); if(s.includes('ad-modal')||s.includes('id=\"toast\"')) throw new Error('残留奖励门/toast'); if(!s.includes('id=\"ad-slot-main\"')) throw new Error('缺广告位容器'); if(!s.includes('/ad-slot.js?v=1.3.0')) throw new Error('未加载 ad-slot.js'); console.log('index.html OK');"`
Expected: `index.html OK`

- [ ] **Step 5: 提交**

```bash
git add public/index.html
git commit -m "feat(ads): index.html retire reward gate, add display ad slot under card" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: app-simple.js — 删奖励门逻辑/引用 + 接广告初始化（必须先于 Task 4 index.html）

**Files:**
- Modify: `public/app-simple.js`（删 19/21-28/42-103/330-336；改 31-33、342-359）

- [ ] **Step 1: 删除 toast 与广告弹窗 DOM 引用**

删除第 19 行：
```js
const toastEl        = document.getElementById("toast");
```
并删除第 21-28 行整段（`// 广告弹窗` 注释 + 7 个 `const ad*` 引用）：
```js
// 广告弹窗
const adModal         = document.getElementById("ad-modal");
const adModalClose    = document.getElementById("ad-modal-close");
const adTomorrowBtn   = document.getElementById("ad-tomorrow-btn");
const adClaimBtn      = document.getElementById("ad-claim-btn");
const adProgressBar   = document.getElementById("ad-progress-bar");
const adCountdownText = document.getElementById("ad-countdown-text");
const adModalError    = document.getElementById("ad-modal-error");
```

- [ ] **Step 2: 删除 toast 函数与整个广告模态框逻辑**

删除第 42-103 行整段（`// ─── Toast 提示` 注释 + `toastTimer`/`showToast`（未被调用）、`// ─── 广告模态框` 注释 + `adTimer`/`AD_WATCH_SECONDS`/`openAdModal`/`closeAdModal`、三个 `adModal*` 监听、`adClaimBtn` 监听）。即从：

```js
// ─── Toast 提示 ────────────────────────────────────────────────────────────
let toastTimer = null;
function showToast(msg) {
```
一直删到：
```js
adClaimBtn.addEventListener("click", () => {
  if (adClaimBtn.disabled) return;
  closeAdModal();
  doCompress();
});
```
（含这段结束的 `});`。）删除后，下一行应是 `// ─── 质量警告气泡 ──...`。

- [ ] **Step 3: 删除大文件拦截分支**

把表单提交里（约 330-338）：

```js
  // 大文件门：仅当广告启用时拦截（前期未接广告 → 直接压缩，仅服务端记录大小）。
  // 看完广告后由 adClaimBtn 回调 doCompress() 继续。
  const isLarge = file.size > LARGE_FILE_MB * 1024 * 1024;
  if (ADS_ENABLED && isLarge) {
    openAdModal();
    return;
  }

  await doCompress();
```

替换为：

```js
  await doCompress();
```

- [ ] **Step 4: 精简配置变量（去掉前端不再用的 LARGE_FILE_MB 与 gate 用途）**

把第 31-33 行：

```js
let LARGE_FILE_MB = 40;   // 大文件阈值（MB）：超过此值的文件，启用广告后需看广告解锁
let ADS_ENABLED   = false; // 广告门是否启用（AD_ENABLED=true）；前期 false → 大文件照常免费
let MAX_UPLOAD_MB = 100;  // 硬上限（MB）：超出直接拒绝
```

替换为：

```js
let ADS_ENABLED   = false; // 是否注入展示广告（AD_ENABLED=true）
let AD_CLIENT     = "";    // AdSense 发布商 ID
let AD_SLOT       = "";    // AdSense 广告单元 ID
let MAX_UPLOAD_MB = 100;  // 硬上限（MB）：超出直接拒绝
```

- [ ] **Step 5: initConfig 读取广告配置并初始化广告位**

把 `initConfig` 整个函数（约 342-359）：

```js
async function initConfig() {
  try {
    const res = await fetch("/api/config");
    if (res.ok) {
      const cfg = await res.json();
      if (typeof cfg.largeFileMB === "number")  LARGE_FILE_MB = cfg.largeFileMB;
      if (typeof cfg.adsEnabled  === "boolean") ADS_ENABLED   = cfg.adsEnabled;
      if (typeof cfg.maxUploadMB === "number")  MAX_UPLOAD_MB = cfg.maxUploadMB;
    }
  } catch {
    // 网络失败时使用默认值（LARGE_FILE_MB=40, ADS_ENABLED=false, MAX_UPLOAD_MB=100）
  }
  // 顶部徽标：不限次数 + 单文件上限（接入广告后可改为"大文件需解锁"提示）
  const usageDisplay = document.getElementById("usage-display");
  if (usageDisplay) {
    usageDisplay.textContent = `免费不限次 · 单文件 ≤ ${MAX_UPLOAD_MB}MB`;
  }
}
```

替换为：

```js
async function initConfig() {
  try {
    const res = await fetch("/api/config");
    if (res.ok) {
      const cfg = await res.json();
      if (typeof cfg.adsEnabled  === "boolean") ADS_ENABLED   = cfg.adsEnabled;
      if (typeof cfg.adClient    === "string")  AD_CLIENT     = cfg.adClient;
      if (typeof cfg.adSlot      === "string")  AD_SLOT       = cfg.adSlot;
      if (typeof cfg.maxUploadMB === "number")  MAX_UPLOAD_MB = cfg.maxUploadMB;
    }
  } catch {
    // 网络失败时使用默认值（ADS_ENABLED=false → 不注入广告）
  }
  const usageDisplay = document.getElementById("usage-display");
  if (usageDisplay) {
    usageDisplay.textContent = `免费不限次 · 单文件 ≤ ${MAX_UPLOAD_MB}MB`;
  }
  // 初始化展示广告位：未启用/未配置 → 不显示、不占空间；启用且有 ID → 注入 AdSense
  if (window.initAdSlot) {
    window.initAdSlot("ad-slot-main", { adsEnabled: ADS_ENABLED, adClient: AD_CLIENT, adSlot: AD_SLOT });
  }
}
```

- [ ] **Step 6: 自检（无残留引用、语法可解析）**

Run: `node -e "const s=require('fs').readFileSync('public/app-simple.js','utf8'); for (const t of ['adModal','openAdModal','showToast','LARGE_FILE_MB','toastEl']) if (s.includes(t)) throw new Error('残留: '+t); if(!s.includes('window.initAdSlot')) throw new Error('未接广告位'); console.log('app-simple.js OK');"`
Expected: `app-simple.js OK`

> 说明：`app-simple.js` 是浏览器脚本（用了 `document`），不能直接 `node -c` 跑（无 DOM）；本步只做静态字符串自检，运行行为在 Task 7 用本地服务 + 手动浏览器验证。

- [ ] **Step 7: 提交**

```bash
git add public/app-simple.js
git commit -m "feat(ads): app-simple.js drop reward-gate/toast, init display ad slot" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: styles.css — 广告位样式

**Files:**
- Modify: `public/styles.css`（文件末尾追加）

- [ ] **Step 1: 追加 .ad-slot 规则**

在 `public/styles.css` **文件末尾**追加：

```css
/* 展示广告位：与主卡片同宽、隐藏时不占空间（hidden 属性即 display:none） */
.ad-slot {
  width: 100%;
  margin: 0 0 20px;
  text-align: center;
  min-height: 0;
}
.ad-slot[hidden] { display: none; }
.ad-slot .adsbygoogle { display: block; }
```

- [ ] **Step 2: 自检**

Run: `node -e "const s=require('fs').readFileSync('public/styles.css','utf8'); if(!s.includes('.ad-slot')) throw new Error('缺 .ad-slot'); console.log('styles.css OK');"`
Expected: `styles.css OK`

- [ ] **Step 3: 提交**

```bash
git add public/styles.css
git commit -m "feat(ads): .ad-slot styles (card-width, collapse when hidden)" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 法务页改写（免费不限次 + AdSense 声明）

**Files:**
- Modify: `public/terms.html`（替换 `<section class="panel legal-card">` 内容）
- Modify: `public/privacy.html`（替换 `<section class="panel legal-card">` 内容）

- [ ] **Step 1: 改写 terms.html**

把 `public/terms.html` 中从 `<h1>用户协议</h1>` 到该 `<section>` 结束前（即第 36 行到第 104 行之间的全部条款内容）替换为：

```html
        <h1>用户协议</h1>
        <p class="update-date">最后更新：2026年6月2日</p>

        <p>欢迎使用 TinyPDF（以下简称"本服务"或"本工具"）。本协议是您与 TinyPDF 团队之间关于使用本服务的法律协议。请在使用本服务前仔细阅读以下条款。<strong>使用本服务即表示您同意本协议的全部内容。</strong>如您不同意本协议，请停止使用本服务。</p>

        <h2>一、服务说明</h2>
        <p>TinyPDF 是一款在线 PDF 文件精准压缩工具，主要功能如下：</p>
        <ul>
          <li>支持将单个 PDF 文件压缩至您指定的目标大小；</li>
          <li>压缩过程保留原文件的页数、页面尺寸和基本版式；</li>
          <li>本工具<strong>不提供</strong> PDF 合并、拆分、格式转换、OCR 识别、编辑或签名等功能；</li>
          <li>本服务仅支持电脑网页版（桌面浏览器），暂不支持移动端。</li>
        </ul>

        <h2>二、使用与免费政策</h2>
        <ul>
          <li>本服务<strong>完全免费、不限次数</strong>，无需注册账号；</li>
          <li>单个文件大小上限为 <strong>100 MB</strong>；</li>
          <li>目标压缩大小必须小于原文件大小；</li>
          <li>禁止使用自动化脚本、爬虫或任何手段对本服务进行批量调用或滥用。</li>
        </ul>

        <h2>三、文件处理规则</h2>
        <ul>
          <li>您上传的 PDF 文件将上传至服务器进行压缩处理，处理完成后文件将在服务器临时存储（最长 <strong>1 小时</strong>），之后自动删除；</li>
          <li>本服务不会长期存储、备份或传播您上传的文件内容；</li>
          <li>压缩结果会尽量接近您指定的目标大小。若文件本身可压缩空间有限，系统将返回尽可能优化的结果，但可能无法完全达到目标大小；</li>
          <li>对于加密、损坏、格式不符或超出系统处理能力的文件，系统可能无法完成压缩并返回错误提示。</li>
        </ul>

        <h2>四、用户责任与合法使用</h2>
        <p>您在使用本服务时，须遵守以下规定：</p>
        <ul>
          <li>您应确保上传的 PDF 文件属于您本人所有，或您已获得合法授权处理该文件；</li>
          <li>禁止上传包含违法、违规、侵权内容的文件，包括但不限于涉及色情、赌博、诈骗、版权侵权等内容；</li>
          <li>禁止将本服务用于任何可能影响服务稳定性的行为；</li>
          <li>您对上传文件的合法性负责，因此引发的任何法律责任由您本人承担。</li>
        </ul>

        <h2>五、广告服务</h2>
        <ul>
          <li>本服务通过展示第三方广告（Google AdSense）维持免费运营，广告以横幅等形式展示于页面，<strong>不影响您免费使用本工具的全部功能</strong>；</li>
          <li>广告内容由第三方广告平台提供，TinyPDF 不对广告内容的准确性、安全性或适宜性负责；</li>
          <li>第三方广告平台可能使用 Cookie 等技术投放与统计广告，详见本站《隐私政策》"第三方广告服务"一节。</li>
        </ul>

        <h2>六、免责声明</h2>
        <ul>
          <li>本服务按"现状"提供，不对压缩效果、服务可用性或结果文件的可用性作任何明示或暗示的保证；</li>
          <li>因不可抗力、网络故障、服务器维护等原因导致服务中断或压缩失败，TinyPDF 不承担责任；</li>
          <li>若因上传文件存在安全漏洞、宏代码等导致的任何损失，TinyPDF 不承担责任；</li>
          <li>在法律允许的最大范围内，TinyPDF 对任何间接损失、数据丢失或业务中断不承担赔偿责任。</li>
        </ul>

        <h2>七、知识产权</h2>
        <p>TinyPDF 的网站设计、代码、商标、Logo 等均为 TinyPDF 团队的知识产权，未经许可不得复制、修改或商业使用。您上传的文件内容的知识产权归您或原权利人所有。</p>

        <h2>八、协议变更</h2>
        <p>TinyPDF 有权随时修改本协议。协议更新后，继续使用本服务即视为您接受修改后的条款。重大变更将在本页面顶部更新"最后更新"日期。</p>

        <h2>九、适用法律</h2>
        <p>本协议受中华人民共和国法律管辖。如发生争议，双方应友好协商解决；协商不成的，提交有管辖权的人民法院诉讼解决。</p>

        <h2>十、联系我们</h2>
        <p>如您对本协议有任何疑问或建议，请通过以下方式联系我们：</p>
        <p>客服邮箱：<a href="mailto:zijinnmengli@gmail.com">zijinnmengli@gmail.com</a></p>
```

- [ ] **Step 2: 改写 privacy.html**

把 `public/privacy.html` 中从 `<h1>隐私政策</h1>` 到 `<h2>六、儿童隐私</h2>` 之前的全部内容（约第 40-119 行）替换为：

```html
        <h1>隐私政策</h1>
        <p class="update-date">最后更新：2026年6月2日</p>

        <p>TinyPDF（以下简称"我们"）非常重视您的隐私。本隐私政策说明我们如何收集、使用和保护您在使用本服务过程中涉及的信息。<strong>使用本服务即表示您同意本政策描述的数据处理方式。</strong></p>

        <h2>一、我们收集哪些信息</h2>
        <p>TinyPDF 坚持最小化数据原则。我们的收集分为以下两类：</p>

        <h2>1.1 您主动提供的信息</h2>
        <ul>
          <li><strong>上传的 PDF 文件：</strong>您选择上传的文件内容，仅用于完成本次压缩任务，压缩完成后在服务器最长保留 <strong>1 小时</strong>，之后自动删除，不做长期存储；</li>
          <li><strong>目标大小参数：</strong>您输入的压缩目标（MB），仅用于当次任务处理，不记录到数据库。</li>
        </ul>

        <h2>1.2 自动收集的信息</h2>
        <ul>
          <li><strong>服务器日志：</strong>包含请求时间、IP 地址、请求路径、HTTP 状态码等标准访问日志，用于排查故障和统计访问量，保留时间不超过 30 天；</li>
          <li><strong>统计分析（Google Analytics）：</strong>我们使用 Google Analytics 4 统计访问量与功能使用情况，并通过第一方 Cookie <code>tinypdf_cid</code>（保留约 2 年）区分访客以避免重复计数。该统计数据为匿名汇总，不包含您的文件内容或个人身份信息；</li>
          <li><strong>第三方广告数据：</strong>页面展示第三方广告（Google AdSense）。广告平台可能依据其隐私政策使用 Cookie 收集设备标识符、IP 地址或浏览行为，用于广告投放与统计（详见第五条）。</li>
        </ul>

        <h2>二、我们如何使用这些信息</h2>
        <table>
          <thead>
            <tr><th>信息类型</th><th>使用目的</th><th>存储位置</th><th>保留时长</th></tr>
          </thead>
          <tbody>
            <tr><td>上传的 PDF 文件</td><td>执行压缩任务</td><td>服务器临时目录</td><td>最长 1 小时，自动删除</td></tr>
            <tr><td>压缩结果文件</td><td>供用户下载</td><td>服务器临时目录</td><td>最长 1 小时，自动删除</td></tr>
            <tr><td>tinypdf_cid（第一方 Cookie）</td><td>访客去重统计</td><td>您的浏览器</td><td>约 2 年</td></tr>
            <tr><td>服务器访问日志</td><td>故障排查、访问统计</td><td>服务器日志</td><td>最长 30 天</td></tr>
            <tr><td>第三方广告 Cookie</td><td>广告投放与统计</td><td>由广告平台管理</td><td>依广告平台政策</td></tr>
          </tbody>
        </table>

        <h2>三、我们不会做的事</h2>
        <ul>
          <li>不读取、分析或存储您 PDF 文件的内容；</li>
          <li>不要求您注册账号或提供任何个人身份信息；</li>
          <li>不将您的文件或使用数据出售、出租或转让给任何第三方；</li>
          <li>不使用您的文件训练 AI 模型或用于本次压缩以外的任何目的；</li>
          <li>除统计与第三方广告所需的 Cookie 外（见上文及第五条），不设置其他追踪 Cookie。</li>
        </ul>

        <h2>四、数据安全</h2>
        <ul>
          <li>所有文件传输通过 HTTPS 加密，防止传输过程中的数据泄露；</li>
          <li>服务器临时文件存储在隔离目录中，仅处理进程可访问；</li>
          <li>系统自动清理超过 1 小时的临时文件；</li>
          <li>尽管我们采取了合理的安全措施，但互联网传输本身存在固有风险，我们无法对绝对安全作出保证。</li>
        </ul>

        <h2>五、第三方广告服务</h2>
        <p>本服务通过展示第三方广告（Google AdSense）维持免费运营。Google 及其合作伙伴可能使用 Cookie 根据您对本站及其他网站的访问，向您投放广告。</p>
        <ul>
          <li>Google 使用广告 Cookie 可帮助其及其合作伙伴基于您的访问投放广告；</li>
          <li>您可在 <a href="https://www.google.com/settings/ads" target="_blank" rel="noopener">Google 广告设置</a> 中管理或关闭个性化广告；</li>
          <li>关于 Google 如何在投放广告时使用数据，详见 <a href="https://policies.google.com/technologies/ads" target="_blank" rel="noopener">Google 广告政策</a>；</li>
          <li>TinyPDF 不控制也不负责第三方广告平台的数据收集行为。</li>
        </ul>
```

（第 119 行起的 `<h2>六、儿童隐私</h2>` 及之后内容保持不变。）

- [ ] **Step 3: 自检**

Run: `node -e "for (const f of ['terms','privacy']){const s=require('fs').readFileSync('public/'+f+'.html','utf8'); for(const t of ['每日','3 次','10 次','localStorage','观看广告']) if(s.includes(t)) throw new Error(f+' 残留旧表述: '+t); if(!s.includes('AdSense')) throw new Error(f+' 缺 AdSense 声明');} console.log('legal OK');"`
Expected: `legal OK`

- [ ] **Step 4: 提交**

```bash
git add public/terms.html public/privacy.html
git commit -m "docs(legal): rewrite terms/privacy for free-unlimited + AdSense disclosure" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: test 脚本 + 整体验证

**Files:**
- Modify: `package.json`（test 脚本）

- [ ] **Step 1: test 脚本串联**

把 `package.json` 的：

```json
    "test": "node test/compress-search.test.js"
```

替换为：

```json
    "test": "node test/compress-search.test.js && node test/ads.test.js"
```

- [ ] **Step 2: 跑全部单测/集成**

Run: `npm test`
Expected: 先 `7/7 passed`（compress-search），再 `13/13 passed`（ads）。

- [ ] **Step 3: 本地服务 + 结构验证（禁用态）**

Run:
```bash
PORT=3777 HOST=127.0.0.1 node server-simple.js > /tmp/ads-smoke.log 2>&1 & SRV=$!
sleep 1
echo "--- /api/config ---"; curl -s http://127.0.0.1:3777/api/config
echo; echo "--- CSP on / ---"; curl -s -D - -o /dev/null http://127.0.0.1:3777/ | grep -i content-security-policy
echo "--- 首页含广告位容器、无奖励门 ---"; curl -s http://127.0.0.1:3777/ | grep -c 'id="ad-slot-main"'; curl -s http://127.0.0.1:3777/ | grep -c 'ad-modal'
kill $SRV 2>/dev/null
```
Expected: config 含 `"adClient":"","adSlot":""`；CSP **不含** googlesyndication；`ad-slot-main` 计数 `1`；`ad-modal` 计数 `0`。

- [ ] **Step 4: 手动浏览器验证（人工，必须）**

1. 禁用态：浏览器打开本地首页 → **看不到任何广告位空白**（容器 hidden，不占空间）；Console 无报错；大文件（>40MB）选中后点压缩 → **直接压缩、无弹窗**。
2. 启用+假ID：`AD_ENABLED=true AD_CLIENT=ca-pub-test AD_SLOT=123 PORT=3777 node server-simple.js` → 打开首页 → Network 里出现对 `pagead2.googlesyndication.com` 的请求；因无真账号 `data-ad-status=unfilled` → 广告位**仍隐藏不占空间**（验证降级）。
3. 法务页 `/terms`、`/privacy` 通读：无"每日 3 次/看广告换次数/localStorage 配额"，含 AdSense/Cookie 声明与退出链接。

- [ ] **Step 5: 提交**

```bash
git add package.json
git commit -m "chore(ads): run ads test in npm test" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完成后（不在单个 Task 内）

- 汇报：单测/集成数字 + 手动验证结果（禁用态无空位、降级隐藏、法务页已更新）。
- **等用户明确批准**后再 `git push`（推送即触发 Railway 部署）。本期 `AD_ENABLED` 默认不设 → 线上无任何外部广告脚本、CSP 维持严格、页面纯免费。
- 用户并行申请 AdSense；过审后在 Railway 设 `AD_ENABLED=true AD_CLIENT=ca-pub-xxxx AD_SLOT=xxxx` 即出真广告（无需改代码）。
- 复用到下一个产品：拷 `public/ad-slot.js` + 广告位容器 div + `/api/config` 三字段 + 动态 CSP 那段 + 法务页广告声明段。
