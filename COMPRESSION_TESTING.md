# PDF压缩逻辑测试说明

## 修复的问题

### 问题1：压缩结果不是最接近目标大小的
**原问题：** 只要找到第一个满足 `bytes <= targetBytes` 的结果就立即返回，可能导致文件被过度压缩。

**修复方案：** 
- 收集所有满足条件的压缩结果
- 按文件大小从大到小排序
- 选择最大的那个（最接近目标大小但不超过目标）

### 问题2：缺少对无效压缩结果的验证
**原问题：** 没有检查压缩后的文件大小是否为0或无效。

**修复方案：**
- 在每次压缩后验证 `bytes > 0`
- 跳过无效的压缩结果

## 压缩策略

### 阶段1：质量压缩（Quality Compression）
使用 `QUALITY_STEPS` 参数，逐步降低图片质量和缩放比例：
- 从最高质量开始（scale: 1.0, quality: 0.98）
- 逐步降低到最低质量（scale: 0.2, quality: 0.2）
- 收集所有满足目标大小的结果，选择最接近的

### 阶段2：光栅化压缩（Raster Compression）
如果质量压缩无法达到目标，使用 `RASTER_STEPS` 进行深度压缩：
- 将PDF页面转换为图片再重新合成PDF
- 从高DPI开始逐步降低
- 最终可能转为灰度模式
- 同样收集所有有效结果，选择最接近的

### 阶段3：插值优化（Interpolation Refinement）
在已有的最佳结果基础上进行细化：
- 在"刚好超过目标"和"刚好低于目标"之间进行插值
- 进行3-4轮优化
- 寻找更精确的平衡点

## 测试用例

### 测试1：普通PDF文件
- **原文件大小：** 28.5 MB
- **目标大小：** 12 MB
- **预期结果：** 11-12 MB之间，尽可能接近12 MB

### 测试2：已经很小的PDF
- **原文件大小：** 5 MB
- **目标大小：** 10 MB
- **预期结果：** 直接返回原文件，不进行压缩

### 测试3：压缩空间很小的PDF
- **原文件大小：** 15 MB
- **目标大小：** 5 MB
- **预期结果：** 返回能达到的最小结果，并提示用户提高目标大小

### 测试4：边界情况
- **原文件大小：** 10.1 MB
- **目标大小：** 10.0 MB
- **预期结果：** 刚好压缩到10 MB或略低于

## 验证要点

1. **结果接近性：** 压缩结果应该是所有有效结果中最大的那个（最接近目标）
2. **有效性：** 压缩后的文件必须 > 0 bytes
3. **不超过目标：** 结果必须 ≤ 目标大小
4. **质量平衡：** 在满足大小要求的前提下，尽可能保持最高质量

## 使用建议

1. 设置合理的目标大小，不要过度压缩
2. 如果系统提示无法达到目标，适当提高目标大小
3. 对于已经压缩过的PDF，再次压缩的空间可能有限
4. 包含大量图片的PDF压缩效果最好
