# PDF 压缩工具维护记录

## 当前最终状态

- 对外可访问地址：
  - `https://zijinmengli-svg.github.io/pdf-compress-web/`
- 当前线上形态：
  - 纯前端 PDF 压缩工具
  - 托管在 GitHub Pages
  - 不依赖本地 Node 服务
  - 不依赖 ngrok
- 当前统计方案：
  - Microsoft Clarity
  - Project ID: `wf7kh62n2p`

## 最终保留目录

- 主项目源码目录：
  - `~/Desktop/PDF压缩工具-最终版/`
- GitHub Pages 前端发布文件：
  - `public-for-github/`
- 当前维护说明：
  - `维护记录.md`

## 关键结论

### 1. 当前线上版本是纯前端版

- 所有 PDF 压缩都在浏览器内完成
- 使用：
  - `pdf.js`
  - `jsPDF`
- 特点：
  - 可以直接分享网址给别人使用
  - 不需要本地电脑持续开机
  - 不需要后端地址输入

### 2. 当前版本的能力边界

- 适合：
  - 快速分享
  - 普通 PDF 目标大小压缩
  - 无后台、无登录、无服务器维护
- 限制：
  - 会把页面重编码为图片型 PDF
  - 文字可复制性和矢量保真度不如原文件
  - 超大文件、复杂 PDF、旧浏览器可能失败或变慢

### 3. 当前统计埋点

- 自动统计：
  - 页面访问
- 自定义事件：
  - `select_pdf_file`
  - `start_compress`
  - `compress_success`
  - `compress_failed`
  - `compress_validation_failed`
  - `download_compressed_pdf`

### 4. 当前埋点维度

- 文件大小区间
- 目标大小区间
- 页数区间
- 压缩后大小区间
- 压缩比例区间
- 上传方式：
  - `picker`
  - `drop`
- 失败原因归类

## 当前重要文件

### GitHub Pages 发布文件

- `public-for-github/index.html`
- `public-for-github/app-github.js`
- `public-for-github/styles.css`
- `public-for-github/favicon.ico`

### 主站相关代码

- `public/`
- `server.js`
- `server-simple.js`
- `scripts/compress_pdf.swift`
- `scripts/rasterize_pdf.swift`
- `scripts/inspect_pdf.swift`

## 已完成的重要调整

### 1. GitHub Pages 发布

- 已成功发布到 `gh-pages`
- 仓库：
  - `https://github.com/zijinmengli-svg/pdf-compress-web`

### 2. Clarity 接入

- 已接入 Microsoft Clarity 脚本
- 已完成关键行为埋点

### 3. favicon 处理

- 已补 `favicon.ico`
- 已避免页面常见的 `favicon 404`

## 后续维护建议

### 如果线上突然打不开

优先检查：

1. `https://zijinmengli-svg.github.io/pdf-compress-web/` 是否返回 `200`
2. GitHub Pages 是否仍然开启
3. `gh-pages` 分支是否还在
4. CDN 资源是否可访问：
   - `pdf.js`
   - `jsPDF`

### 如果 Clarity 没数据

优先检查：

1. 页面是否已真实访问
2. 浏览器是否拦截统计脚本
3. Clarity 项目是否还是 `wf7kh62n2p`

### 如果要继续优化产品

优先级建议：

1. 补本地依赖文件，减少对 CDN 的依赖
2. 加压缩档位：
   - 轻度
   - 标准
   - 极限
3. 加失败重试与更明确的错误提示
4. 做移动端体验优化

## 这次桌面整理的原则

- 保留唯一主项目目录
- 删除重复旧版本目录
- 删除独立前端副本
- 删除过时操作记录副本
- 所有后续维护以本目录为准
