# 保分辨率优先压缩 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 TinyPDF 在目标大小以内输出"保住原生分辨率、质量为辅"的最高清 PDF，修掉"20MB 目标只给 11MB / 分辨率被砍"的问题。

**Architecture:** 把"找最优(分辨率,质量)配置"抽成纯函数 `lib/compress-search.js`（注入 `probe` 回调，零 I/O、可单测）；`server-simple.js` 注入真实 Ghostscript probe 调用它，命中后用选定配置渲染最终输出。分辨率优先：固定高分辨率 cap，先搜 JPEG 质量贴目标下沿；只有全分辨率下质量降到地板(~q70)仍超目标时，才沿分辨率阶梯下降。

**Tech Stack:** Node.js（零 npm 依赖）、Ghostscript 10.x（`pdfwrite` + 强制 DCTEncode + setdistillerparams QFactor）、Node 内置 `assert`（单测）。

参考 spec：`docs/superpowers/specs/2026-05-31-压缩保分辨率优先-design.md`

---

## File Structure

- **Create** `lib/compress-search.js` — 纯搜索逻辑：`COMPRESS` 默认常量、`qfBracket`、`searchBestConfig`。无副作用、无 I/O。
- **Create** `test/compress-search.test.js` — 用合成体积模型(oracle)单测纯逻辑（零依赖）。
- **Modify** `server-simple.js` — require lib；`runGsQf` 加下采样阈值；删除旧 `qfactorSearch`、新增 `vectorCompressSearch`（真实 probe + 调用 `searchBestConfig`）；`compressPdf` 改用新搜索并用选定配置渲染最终输出；`cleanupJob` 清理新临时文件名；进度文案。
- **Modify** `package.json` — 加 `"test"` 脚本。
- **Create (throwaway)** `/tmp/integration-compress.js` — 起真实服务、POST 用户真实文件、轮询 SSE，断言体积/时间不变量。

---

## Task 1: 纯函数 `qfBracket`（定分辨率搜质量，贴目标下沿）

**Files:**
- Create: `test/compress-search.test.js`
- Create: `lib/compress-search.js`

- [ ] **Step 1: 写失败测试**

写入 `test/compress-search.test.js`：

```js
"use strict";
const assert = require("assert");
const { COMPRESS, qfBracket, searchBestConfig } = require("../lib/compress-search");

// 合成体积模型：体积随 qf 减小而增大(∝ qf^-0.5)、随有效分辨率^2 增大；
// 有效分辨率 = min(cap, nativeDpi) —— 模拟"绝不上采样"。
function makeOracle({ base, nativeDpi, refDpi = 150, gamma = 0.5, quantize = 0 }) {
  return async (qf, cap) => {
    const effDpi = Math.min(cap, nativeDpi);
    let bytes = base * Math.pow(effDpi / refDpi, 2) * Math.pow(qf, -gamma);
    if (quantize > 0) bytes = Math.round(bytes / quantize) * quantize; // 模拟 JPEG 量化台阶
    return Math.round(bytes);
  };
}

let passed = 0, failed = 0;
async function test(name, fn) {
  try { await fn(); console.log("PASS - " + name); passed++; }
  catch (e) { console.log("FAIL - " + name + " :: " + e.message); failed++; }
}

(async () => {
  // qfBracket：低清文件(native 130)，目标 20e6，全分辨率(cap 600→eff 130)下质量地板可达标。
  await test("qfBracket 命中目标内、贴下沿、保住分辨率", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 130 });
    const target = 20e6;
    const r = await qfBracket(probe, 600, COMPRESS.QF_BEST, COMPRESS.QF_FLOOR, target);
    assert.ok(r, "应返回配置");
    assert.strictEqual(r.resCap, 600, "分辨率 cap 应保持 600（不下采样）");
    assert.ok(r.bytes <= target, `不得超目标: ${r.bytes} <= ${target}`);
    assert.ok(r.bytes >= target * 0.8, `应贴近目标下沿: ${r.bytes} >= ${target * 0.8}`);
    assert.ok(r.qf > COMPRESS.QF_BEST && r.qf < COMPRESS.QF_FLOOR, "qf 应落在区间内");
  });

  // qfBracket：最高质量已达标(有余量) → 直接返回最高质量，不注水。
  await test("qfBracket 最高质量已达标则返回最高质量", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 130 });
    const r = await qfBracket(probe, 600, COMPRESS.QF_BEST, COMPRESS.QF_FLOOR, 40e6);
    assert.ok(r, "应返回配置");
    assert.strictEqual(r.qf, COMPRESS.QF_BEST, "最高质量已 ≤ 目标，应返回最高质量");
    assert.ok(r.bytes <= 40e6);
  });

  // qfBracket：连质量地板都超目标 → null。
  await test("qfBracket 地板仍超目标返回 null", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 600 }); // 高清，cap600 体积大
    const r = await qfBracket(probe, 600, COMPRESS.QF_BEST, COMPRESS.QF_FLOOR, 8e6);
    assert.strictEqual(r, null, "全分辨率地板都塞不进 → null");
  });

  // qfBracket：量化台阶下也绝不返回超目标结果（修边界 bug）。
  await test("qfBracket 量化台阶下仍 ≤ 目标", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 130, quantize: 2e6 });
    const target = 20e6;
    const r = await qfBracket(probe, 600, COMPRESS.QF_BEST, COMPRESS.QF_FLOOR, target);
    assert.ok(r, "应返回配置");
    assert.ok(r.bytes <= target, `量化下也不得超目标: ${r.bytes} <= ${target}`);
  });

  console.log(`\nSUMMARY: ${passed}/${passed + failed} passed`);
  process.exit(failed === 0 ? 0 : 1);
})();
```

- [ ] **Step 2: 运行确认失败**

Run: `node test/compress-search.test.js`
Expected: FAIL —— `Cannot find module '../lib/compress-search'`

- [ ] **Step 3: 实现 lib（常量 + qfBracket）**

写入 `lib/compress-search.js`：

```js
"use strict";
// 纯函数：在体积预算内搜索"最高清"的(分辨率,质量)配置。无 I/O，调用方注入 async probe。
// 分辨率优先：保住分辨率，优先用质量换体积；全分辨率下质量降到地板仍超目标才降分辨率。

const COMPRESS = {
  QF_BEST: 0.02,   // 最高 JPEG 质量（体积最大）
  QF_FLOOR: 0.4,   // 最低可接受质量(~q70)；低于此宁可降分辨率
  QF_WORST: 3.0,   // 绝对最低质量，仅尽力而为兜底用
  RES_CEIL: 600,   // 分辨率上限(DPI)：保原生、收极端高清图
  RES_FLOOR: 72,   // 最低下采样分辨率
  RES_LADDER: [600, 450, 300, 220, 150, 100, 72], // 自高到低尝试的分辨率 cap
  DOWNSAMPLE_THRESHOLD: 1.0, // gs 阈值：让 cap 真正生效、绝不上采样
};

// 定分辨率 cap，找"质量最高(qf 最小)且体积 ≤ target"的配置。
// probe(qf, resCap) -> Promise<number|null>（字节数，失败/无效返回 null）。
// qfLo=最高质量，qfHi=最低可接受质量。返回 { qf, resCap, bytes }(≤target) 或 null(连 qfHi 都超)。
async function qfBracket(probe, resCap, qfLo, qfHi, target, maxIters = 5) {
  const hi = await probe(qfHi, resCap);
  if (hi == null || hi > target) return null;            // 连最低可接受质量都超目标
  let best = { qf: qfHi, resCap, bytes: hi };            // 已知 ≤ target 的兜底
  const lo = await probe(qfLo, resCap);
  if (lo != null && lo <= target) return { qf: qfLo, resCap, bytes: lo }; // 最高质量已达标
  let over = { qf: qfLo, bytes: (lo == null ? Infinity : lo) };
  for (let i = 0; i < maxIters; i++) {
    if (best.qf - over.qf < 0.01) break;                 // 区间足够小
    const p = Math.log(over.bytes / best.bytes) / Math.log(best.qf / over.qf);
    let q = (!isFinite(p) || p <= 0)
      ? (over.qf + best.qf) / 2                           // 退化 → 二分
      : over.qf * Math.pow(over.bytes / target, 1 / p);  // 幂律预测命中目标
    const margin = (best.qf - over.qf) * 0.05;           // 强制步进、严格夹在括内
    q = Math.min(best.qf - margin, Math.max(over.qf + margin, q));
    const r = await probe(q, resCap);
    if (r != null && r <= target) best = { qf: q, resCap, bytes: r };
    else over = { qf: q, bytes: (r == null ? over.bytes : r) };
  }
  return best;
}

module.exports = { COMPRESS, qfBracket };
```

> 注：本步 export 暂不含 `searchBestConfig`（Task 2 才加）。测试文件已 import 它但本步用例不调用它（值为 `undefined`，无影响）；本步只覆盖 `qfBracket`。

- [ ] **Step 4: 运行确认通过**

Run: `node test/compress-search.test.js`
Expected: PASS —— `SUMMARY: 4/4 passed`

- [ ] **Step 5: 提交**

```bash
git add lib/compress-search.js test/compress-search.test.js
git commit -m "feat(compress): pure qfBracket quality search (resolution fixed)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 纯函数 `searchBestConfig`（分辨率优先 + 兜底）

**Files:**
- Modify: `lib/compress-search.js`
- Modify: `test/compress-search.test.js`

- [ ] **Step 1: 追加失败测试**

在 `test/compress-search.test.js` 的 `console.log("\nSUMMARY...` 之前插入：

```js
  // searchBestConfig：低清文件全分辨率达标 → 保住 600 cap。
  await test("searchBestConfig 低清文件保住全分辨率", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 130 });
    const r = await searchBestConfig(probe, 20e6, COMPRESS);
    assert.ok(r, "应返回配置");
    assert.strictEqual(r.resCap, 600, "低清文件应保 600 cap");
    assert.ok(r.bytes <= 20e6);
  });

  // searchBestConfig：高清文件全分辨率塞不下 → 沿阶梯下降取最高可行 cap。
  await test("searchBestConfig 高清文件取最高可行分辨率", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 600 });
    const r = await searchBestConfig(probe, 8e6, COMPRESS);
    assert.ok(r, "应返回配置");
    assert.strictEqual(r.resCap, 100, "应取能塞进目标的最高 cap(=100)");
    assert.ok(r.bytes <= 8e6, `不得超目标: ${r.bytes}`);
  });

  // searchBestConfig：物理不可达(目标过小) → null（交由调用方栅格化）。
  await test("searchBestConfig 不可达返回 null", async () => {
    const probe = makeOracle({ base: 6e6, nativeDpi: 600 });
    const r = await searchBestConfig(probe, 5e5, COMPRESS);
    assert.strictEqual(r, null, "目标过小、连兜底都超 → null");
  });
```

并把文件顶部 import 行保持不变（已 import `searchBestConfig`）。

- [ ] **Step 2: 运行确认失败**

Run: `node test/compress-search.test.js`
Expected: FAIL —— `searchBestConfig is not a function`（共 7 个用例，3 个新用例失败）

- [ ] **Step 3: 实现 searchBestConfig 并修正 export**

在 `lib/compress-search.js` 中，把 `module.exports` 行上方插入：

```js
// 分辨率优先：沿分辨率阶梯自高到低，第一个"质量地板内可塞进目标"的 cap 即用之搜质量贴目标。
// 全程返回 { qf, resCap, bytes }(≤target) 或 null（任何分辨率都塞不下 → 调用方栅格化）。
async function searchBestConfig(probe, target, opts = COMPRESS) {
  const { QF_BEST, QF_FLOOR, QF_WORST, RES_LADDER, RES_FLOOR } = opts;
  for (const cap of RES_LADDER) {
    const floorBytes = await probe(QF_FLOOR, cap);       // 该分辨率下最低可接受质量
    if (floorBytes != null && floorBytes <= target) {
      const r = await qfBracket(probe, cap, QF_BEST, QF_FLOOR, target);
      if (r) return r;                                   // 能塞进目标的最高分辨率
    }
  }
  // 任何分辨率的质量地板都超目标 → 在最低分辨率放开质量尽力而为。
  return await qfBracket(probe, RES_FLOOR, QF_BEST, QF_WORST, target);
}
```

并把最后一行 export 改为：

```js
module.exports = { COMPRESS, qfBracket, searchBestConfig };
```

- [ ] **Step 4: 运行确认通过**

Run: `node test/compress-search.test.js`
Expected: PASS —— `SUMMARY: 7/7 passed`

- [ ] **Step 5: 提交**

```bash
git add lib/compress-search.js test/compress-search.test.js
git commit -m "feat(compress): resolution-first searchBestConfig over the DPI ladder

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 接入 `server-simple.js`

**Files:**
- Modify: `server-simple.js:9`（require）
- Modify: `server-simple.js:340-341`（runGsQf 阈值）
- Modify: `server-simple.js:361-418`（删旧 qfactorSearch、加 vectorCompressSearch）
- Modify: `server-simple.js:449-456`（cleanupJob）
- Modify: `server-simple.js:503-516`（compressPdf 主搜索块）

- [ ] **Step 1: require lib**

在第 9 行 `const { URL } = require("url");` 之后新增一行：

```js
const { COMPRESS, searchBestConfig } = require("./lib/compress-search");
```

- [ ] **Step 2: runGsQf 加下采样阈值**

把 `server-simple.js` 中这两行（约 340-341）：

```js
      "-dDownsampleColorImages=true", `-dColorImageResolution=${resCap}`,
      "-dDownsampleGrayImages=true", `-dGrayImageResolution=${resCap}`,
```

替换为：

```js
      "-dDownsampleColorImages=true", `-dColorImageResolution=${resCap}`,
      "-dDownsampleGrayImages=true", `-dGrayImageResolution=${resCap}`,
      `-dColorImageDownsampleThreshold=${COMPRESS.DOWNSAMPLE_THRESHOLD}`,
      `-dGrayImageDownsampleThreshold=${COMPRESS.DOWNSAMPLE_THRESHOLD}`,
```

- [ ] **Step 3: 用 vectorCompressSearch 替换整个 qfactorSearch 函数**

删除现有 `async function qfactorSearch(...) { ... }`（约 355-418，含其上方注释块），替换为：

```js
// 分辨率优先的矢量压缩搜索：构造真实 Ghostscript probe，交由纯函数 searchBestConfig 选最优
// (分辨率,质量) 配置。probe 即用即删探测临时文件，返回字节数或 null。
// 返回 { qf, resCap, bytes }(≤target) 或 null（交由栅格化兜底）。不落地最终文件，由调用方按配置渲染。
async function vectorCompressSearch(jobId, job, inputPath, scratchBase, targetBytes, minValidBytes) {
  let step = 0;
  const scratch = `${scratchBase}.probe.tmp`;
  const probe = async (qf, resCap) => {
    job.state.progress = Math.min(0.8, 0.12 + step * 0.05);
    job.state.message  = "正在尝试符合要求的最高清版本…";
    sendEvent(jobId, job.state);
    step++;
    try { await runGsQf(inputPath, scratch, qf, resCap); }
    catch { try { fs.unlinkSync(scratch); } catch {} return null; }
    let st; try { st = await fsp.stat(scratch); } catch { return null; }
    try { fs.unlinkSync(scratch); } catch {}
    if (st.size < minValidBytes) return null;
    return st.size;
  };
  return await searchBestConfig(probe, targetBytes, COMPRESS);
}
```

- [ ] **Step 4: cleanupJob 清理新临时文件**

把 `cleanupJob` 中这段（约 454-456）：

```js
    // 删除所有可能泄漏的中间临时文件（QFactor 搜索遗留）
    const base = job.outputPath;
    for (let i = 0; i < 10; i++) { try { fs.unlinkSync(`${base}.qf${i}.tmp`); } catch {} }
```

替换为：

```js
    // 删除可能泄漏的探测临时文件
    try { fs.unlinkSync(`${job.outputPath}.probe.tmp`); } catch {}
```

- [ ] **Step 5: compressPdf 改用新搜索 + 按配置渲染最终输出**

把 compressPdf 中这段（约 501-516，从注释 `// ── 主压缩` 到其 `catch {}` 结束）：

```js
    // ── 主压缩：QFactor 连续质量搜索（矢量保真，目标内体积最大 = 最清晰）──
    // 在 QFactor 维度预测式搜索，命中 <= 目标的最大体积；文字/矢量保持清晰不栅格化。
    if (resultBytes === null) {
      try {
        const qres = await qfactorSearch(jobId, job, inputPath, outputPath, targetBytes, MIN_VALID_BYTES);
        if (qres && qres.bytes >= MIN_VALID_BYTES) {
          if (qres.path !== outputPath) await fsp.rename(qres.path, outputPath);
          resultBytes    = qres.bytes;
          ratio          = resultBytes / originalBytes;
          bestValidPath  = outputPath;
          bestValidBytes = resultBytes;
        }
      } catch {
        // 搜索失败 → 交由栅格化兜底
      }
    }
```

替换为：

```js
    // ── 主压缩：分辨率优先搜索（保住原生分辨率，质量为辅；目标内最清晰，矢量不栅格化）──
    if (resultBytes === null) {
      try {
        const cfg = await vectorCompressSearch(jobId, job, inputPath, outputPath, targetBytes, MIN_VALID_BYTES);
        if (cfg) {
          await runGsQf(inputPath, outputPath, cfg.qf, cfg.resCap); // 用选定配置渲染最终输出
          const st = await fsp.stat(outputPath);
          if (st.size <= targetBytes && st.size >= MIN_VALID_BYTES) {
            resultBytes    = st.size;
            ratio          = resultBytes / originalBytes;
            bestValidPath  = outputPath;
            bestValidBytes = resultBytes;
          }
        }
      } catch {
        // 搜索/渲染失败 → 交由栅格化兜底
      }
    }
```

- [ ] **Step 6: 语法检查**

Run: `node -c server-simple.js`
Expected: 无输出（退出码 0）

- [ ] **Step 7: 启动冒烟（确认 require lib 不报错、服务可起）**

Run:
```bash
PORT=3599 node server-simple.js & SRV=$!; sleep 1; curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3599/api/config; kill $SRV
```
Expected: `200`

- [ ] **Step 8: 提交**

```bash
git add server-simple.js
git commit -m "feat(compress): wire resolution-first search into server, render chosen config

- runGsQf: add DownsampleThreshold=1.0 so the DPI cap actually bites
- replace qfactorSearch(300 hardcoded) with vectorCompressSearch over searchBestConfig
- compressPdf renders the chosen {qf,resCap}; progress shows highest-clarity attempt

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 测试脚本 + 真实文件集成验证 + 回归

**Files:**
- Modify: `package.json`（scripts.test）
- Create (throwaway): `/tmp/integration-compress.js`

- [ ] **Step 1: 加 test 脚本**

把 `package.json` 的 `"scripts"` 块：

```json
  "scripts": {
    "start": "node server-simple.js",
    "dev": "node server-simple.js"
  },
```

替换为：

```json
  "scripts": {
    "start": "node server-simple.js",
    "dev": "node server-simple.js",
    "test": "node test/compress-search.test.js"
  },
```

- [ ] **Step 2: 跑单测确认绿**

Run: `npm test`
Expected: `SUMMARY: 7/7 passed`

- [ ] **Step 3: 写真实文件集成 harness**

写入 `/tmp/integration-compress.js`：

```js
"use strict";
// 真实文件端到端：起真实服务，POST 用户真实 PDF，轮询 SSE 到 done，断言体积/时间不变量。
const http = require("http");
const fs = require("fs");
const { spawn } = require("child_process");

const SRV_DIR = "/Users/libin/Desktop/我的工作流集合/PDF压缩工具-最终版";
const PORT = 3611;
const DIR = "/Users/libin/Desktop/2026作品集设计/设计作品导出图片";
const CASES = [
  { file: `${DIR}/资深体验设计师-李彬-作品集-社交版.pdf`, targetMB: 20, expectRasterized: false, label: "社交版71.55MB→20MB" },
  { file: `${DIR}/资深体验设计师-李彬-作品集.pdf`,       targetMB: 25, expectPassthrough: true, label: "作品集→25MB(原样放行)" },
  { file: `${DIR}/资深体验设计师-李彬-作品集.pdf`,       targetMB: 5,  label: "作品集→5MB(降分辨率，允许必要时栅格化)" },
];

function post(file, targetMB) {
  const buf = fs.readFileSync(file);
  const b = "----t" + Date.now();
  const pre = Buffer.from(`--${b}\r\nContent-Disposition: form-data; name="pdf"; filename="t.pdf"\r\nContent-Type: application/pdf\r\n\r\n`, "latin1");
  const mid = Buffer.from(`\r\n--${b}\r\nContent-Disposition: form-data; name="targetMB"\r\n\r\n${targetMB}\r\n--${b}--\r\n`, "latin1");
  const body = Buffer.concat([pre, buf, mid]);
  return new Promise((res, rej) => {
    const r = http.request({ host: "127.0.0.1", port: PORT, path: "/api/jobs", method: "POST",
      headers: { "Content-Type": `multipart/form-data; boundary=${b}`, "Content-Length": body.length } },
      (rp) => { let d = ""; rp.on("data", c => d += c); rp.on("end", () => res(JSON.parse(d))); });
    r.on("error", rej); r.write(body); r.end();
  });
}
function waitDone(jobId) {
  return new Promise((res, rej) => {
    const r = http.get({ host: "127.0.0.1", port: PORT, path: `/api/jobs/${jobId}/events` }, (rp) => {
      let buf = "";
      rp.on("data", (c) => {
        buf += c;
        let i; while ((i = buf.indexOf("\n\n")) !== -1) {
          const line = buf.slice(0, i); buf = buf.slice(i + 2);
          const m = line.match(/^data: (.*)$/m); if (!m) continue;
          const st = JSON.parse(m[1]);
          if (st.status === "done" || st.status === "error") { rp.destroy(); res(st); return; }
        }
      });
      rp.on("error", rej);
    });
    r.on("error", rej);
  });
}
const sleep = ms => new Promise(r => setTimeout(r, ms));

(async () => {
  const srv = spawn("node", ["server-simple.js"], { cwd: SRV_DIR, stdio: ["ignore", "ignore", "inherit"], env: { ...process.env, PORT: String(PORT), HOST: "127.0.0.1" } });
  for (let i = 0; i < 50; i++) { try { await new Promise((s, j) => http.get({ host: "127.0.0.1", port: PORT, path: "/api/config" }, r => { r.resume(); s(); }).on("error", j)); break; } catch { await sleep(100); } }
  let ok = 0, ng = 0;
  for (const c of CASES) {
    const t0 = Date.now();
    const job = await post(c.file, c.targetMB);
    const st = await waitDone(job.id);
    const secs = ((Date.now() - t0) / 1000).toFixed(1);
    const okSize = st.status === "done" && st.resultBytes <= st.targetBytes && st.resultBytes <= st.originalBytes;
    const okTime = (Date.now() - t0) < 300000;
    const okRaster = c.expectRasterized === undefined || !!st.rasterized === c.expectRasterized;
    const okPass = !c.expectPassthrough || st.resultBytes === st.originalBytes;
    const good = okSize && okTime && okRaster && okPass;
    console.log(`${good ? "PASS" : "FAIL"} - ${c.label}: ${(st.resultBytes/1048576).toFixed(2)}MB / 目标 ${(st.targetBytes/1048576).toFixed(2)}MB / 原 ${(st.originalBytes/1048576).toFixed(2)}MB / ${secs}s / raster=${!!st.rasterized}${st.error ? " / err=" + st.error : ""}`);
    good ? ok++ : ng++;
  }
  srv.kill("SIGKILL");
  console.log(`\nINTEGRATION: ${ok}/${ok + ng} passed`);
  process.exit(ng === 0 ? 0 : 1);
})().catch(e => { console.error("HARNESS ERROR: " + (e.stack || e)); process.exit(1); });
```

- [ ] **Step 4: 跑集成验证（真实文件，可能 2-4 分钟）**

Run: `node /tmp/integration-compress.js 2>&1 | tee /tmp/integration-out.txt`
Expected: `INTEGRATION: 3/3 passed`，且：
- 社交版→20MB：结果 ~19–19.8MB、`raster=false`、< 300s
- 作品集→25MB：结果 == 原文件（原样放行）
- 作品集→5MB：结果 ≤5MB、`raster=false`

> 若任一 FAIL，进入 systematic-debugging：先看 `/tmp/integration-out.txt` 的实际体积/raster/err，再定位。常见点：阈值参数未生效（确认 Step 2 的 threshold 行）、最终渲染体积与探测不一致（probe 与最终 runGsQf 入参须一致）。

- [ ] **Step 5: 回归 — 现有功能测试仍绿**

Run: `node /tmp/phase1-test.js 2>&1 | tail -3`
Expected: `14/14`（若 `/tmp/phase1-test.js` 已不在，跳过并在汇报中说明）

- [ ] **Step 6: 提交 test 脚本**

```bash
git add package.json
git commit -m "chore: add npm test script for compress-search unit tests

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完成后（不在单个 Task 内）

- 汇报给用户：单测 7/7、集成 3/3 的真实数字（体积/耗时/是否栅格化），并附最终输出文件路径供其再次目检清晰度。
- **等用户明确批准**后再 `git push`（推送即触发 Railway 部署、消耗额度）。未批准不推送。
- 可选后续：在用户首肯下，把"质量地板 QF_FLOOR"按真实输出微调；92.9MB 高清版手动跑一遍确认最坏耗时。
