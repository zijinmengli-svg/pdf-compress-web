# TinyPDF v1.1.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将网站品牌名改为 TinyPDF，并在积分不足时弹出弹窗，提供"知道了"和"获取更多积分"两个操作，广告为5秒倒计时 mock，完播后获得10积分并自动继续压缩。

**Architecture:** 在现有 server-simple.js 中新增 `POST /api/ads/reward` 路由 + `ad_rewards` 表；在 index.html 的现有 `insufficient-card` 中补充两个按钮；新增激励广告模态框；在 app-simple.js 中新增 AdAdapter（mock）和完整的广告奖励流程。

**Tech Stack:** Node.js (raw http), SQLite (sqlite/sqlite3), Vanilla JS, HTML/CSS — 无框架，无构建工具。

---

## 文件变更总览

| 文件 | 操作 | 说明 |
|------|------|------|
| `server-simple.js` | Modify | 品牌名替换；添加 `ad_rewards` 表；添加 `/api/ads/reward` 路由；`/api/config` 新增 `adProvider` 字段 |
| `public/index.html` | Modify | 品牌名替换；`insufficient-card` 添加两个按钮；新增激励广告模态框 HTML |
| `public/app-simple.js` | Modify | 监听两个按钮；AdAdapter mock；倒计时逻辑；`/api/ads/reward` 调用；积分更新后自动重压 |
| `public/admin.html` | Modify | 品牌名替换 |

---

## Task 1: 品牌名全局替换为 TinyPDF

**Files:**
- Modify: `server-simple.js:425,567,659,662`
- Modify: `public/index.html:6,12,13,20,21,23,30,41`
- Modify: `public/admin.html` (标题处)

- [ ] **Step 1: 替换 server-simple.js 中的品牌名**

在 `server-simple.js` 中定位并修改以下4处：

```javascript
// 第 423-430 行 /api/config 响应，将 siteName 改为：
json(res, 200, {
  siteName: "TinyPDF",
  maxUploadMB: 250,
  initialPoints: INITIAL_POINTS,
  pointsPerCompress: POINTS_PER_COMPRESS,
  adProvider: "mock"
});
```

```javascript
// 第 560-569 行 /api/jobs 响应中 config 对象，将 siteName 改为：
config: {
  siteName: "TinyPDF",
  maxUploadMB: 250
}
```

```javascript
// 第 658-659 行邮件 subject，改为：
subject: "TinyPDF - 登录验证码",
```

```javascript
// 第 661-662 行邮件 html 中的文字，改为：
<p style="color:#546079;margin:0 0 24px">用于 TinyPDF 邮箱绑定 / 登录，10分钟内有效。</p>
```

- [ ] **Step 2: 替换 public/index.html 中的品牌名**

将以下内容全部替换：

```html
<!-- <title> 行，改为： -->
<title>TinyPDF - 精准压缩 PDF | 免费在线工具</title>

<!-- meta description，改为： -->
<meta name="description" content="TinyPDF - 精准压缩PDF到指定大小，免费在线使用，无需安装。" />

<!-- OG title，改为： -->
<meta property="og:title" content="TinyPDF - 精准压缩 PDF" />
<meta property="og:description" content="精准压缩PDF到指定大小，免费在线使用，无需安装。" />

<!-- Twitter Card，改为： -->
<meta name="twitter:title" content="TinyPDF - 精准压缩 PDF" />
<meta name="twitter:description" content="精准压缩PDF到指定大小，免费在线使用，无需安装。" />

<!-- header eyebrow span，改为： -->
<span class="eyebrow">TinyPDF</span>

<!-- h1，改为： -->
<h1>想多大就多大<br />TinyPDF 精准压缩</h1>
```

- [ ] **Step 3: 替换 public/admin.html 中的品牌名**

在 `public/admin.html` 中找到 `<title>` 标签，替换为：
```html
<title>TinyPDF 管理后台</title>
```
若 admin.html 中有其他 "PDF压缩神器" 字样，一并替换为 "TinyPDF"。

- [ ] **Step 4: 验证替换结果**

运行以下命令确认没有残留旧品牌名：
```bash
grep -rn "PDF压缩神器\|PDF Compression Skill" public/index.html public/admin.html server-simple.js
```
预期输出：**无任何结果**（空输出）。

- [ ] **Step 5: 启动服务验证**

```bash
cd /Users/libin/Desktop/我的工作流集合/PDF压缩工具-最终版
node server-simple.js
```

打开 http://localhost:3487，确认：
- 浏览器标签页标题显示 "TinyPDF - 精准压缩 PDF"
- 页面 header 显示 "TinyPDF"
- h1 显示 "TinyPDF 精准压缩"
- `Ctrl+C` 停止服务

- [ ] **Step 6: Commit**

```bash
git add server-simple.js public/index.html public/admin.html
git commit -m "feat(v1.1.0): rename brand to TinyPDF globally"
```

---

## Task 2: 后端 — 添加 ad_rewards 表和 /api/ads/reward 路由

**Files:**
- Modify: `server-simple.js:88-125` (initDb 函数，添加建表语句)
- Modify: `server-simple.js:422` (handleApiRequest 函数，添加新路由)

- [ ] **Step 1: 在 initDb() 中添加 ad_rewards 表**

找到 `initDb()` 函数中的 `db.exec(...)` 调用（约第92行），在现有 SQL 末尾、最后一个反引号之前添加建表语句：

```javascript
// 在 CREATE INDEX IF NOT EXISTS idx_transactions_user... 之后添加：

    CREATE TABLE IF NOT EXISTS ad_rewards (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      user_id TEXT NOT NULL,
      slot_id TEXT NOT NULL DEFAULT 'slot_reward',
      points_granted INTEGER NOT NULL DEFAULT 10,
      watch_seconds INTEGER,
      created_at INTEGER NOT NULL
    );

    CREATE INDEX IF NOT EXISTS idx_ad_rewards_user_date
      ON ad_rewards(user_id, created_at);
```

- [ ] **Step 2: 添加 /api/ads/reward 路由**

在 `handleApiRequest` 函数中，找到 `/api/track` 路由之后（约第488行），插入以下完整路由：

```javascript
if (url.pathname === "/api/ads/reward" && req.method === "POST") {
  const session = await authenticate(req);
  if (!session) {
    sendError(res, 401, "UNAUTHORIZED", "请先登录");
    return;
  }

  const body = await parseJson(req);
  const watchSeconds = Number(body?.watch_seconds || 0);
  const slotId = body?.slot_id || "slot_reward";

  // 服务端校验：至少观看10秒
  if (watchSeconds < 10) {
    sendError(res, 400, "WATCH_TOO_SHORT", "观看时间不足");
    return;
  }

  // 每日领取上限：最多10次（100积分）
  const todayStart = new Date();
  todayStart.setHours(0, 0, 0, 0);
  const todayStartTs = todayStart.getTime();

  const todayCount = await db.get(
    "SELECT COUNT(*) as cnt FROM ad_rewards WHERE user_id = ? AND created_at >= ?",
    [session.userId, todayStartTs]
  );

  if (todayCount.cnt >= 10) {
    sendError(res, 429, "REWARD_LIMIT_EXCEEDED", "今日领取次数已达上限（10次）");
    return;
  }

  const REWARD_POINTS = 10;
  const result = await addPoints(session, REWARD_POINTS, "激励广告奖励");

  await db.run(
    "INSERT INTO ad_rewards (user_id, slot_id, points_granted, watch_seconds, created_at) VALUES (?, ?, ?, ?, ?)",
    [session.userId, slotId, REWARD_POINTS, watchSeconds, Date.now()]
  );

  json(res, 200, {
    success: true,
    points_added: REWARD_POINTS,
    new_balance: result.points
  });
  return;
}
```

- [ ] **Step 3: 验证路由是否工作**

启动服务：
```bash
node server-simple.js
```

在另一个终端，先获取 token：
```bash
curl -s -X POST http://localhost:3487/api/auth/anonymous \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"test-device-001"}' | python3 -m json.tool
```
记录返回的 `token` 值，然后测试广告奖励接口（把 `YOUR_TOKEN` 替换为实际值）：
```bash
curl -s -X POST http://localhost:3487/api/ads/reward \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"slot_id":"slot_reward","watch_seconds":5}' | python3 -m json.tool
```
预期响应：
```json
{
  "success": true,
  "points_added": 10,
  "new_balance": 20
}
```

再次调用4次，第4次应返回：
```json
{
  "code": "REWARD_LIMIT_EXCEEDED",
  "message": "今日领取次数已达上限（10次）"
}
```

`Ctrl+C` 停止服务。

- [ ] **Step 4: Commit**

```bash
git add server-simple.js
git commit -m "feat(v1.1.0): add ad_rewards table and POST /api/ads/reward route"
```

---

## Task 3: 前端 HTML — 升级 insufficient-card 并新增激励广告模态框

**Files:**
- Modify: `public/index.html:89-113` (insufficient-card section)
- Modify: `public/index.html:159` (在 `</body>` 前新增广告模态框)

- [ ] **Step 1: 升级 insufficient-card**

找到 `public/index.html` 中的 `insufficient-card` section（第89-113行），替换为：

```html
<!-- 积分不足提示 -->
<section class="insufficient-card" id="insufficient-card" hidden>
  <h2>积分不足</h2>
  <p id="insufficient-msg">每次压缩需要 10 积分，当前积分不足。<br>看一个广告可获得 10 积分（1次压缩机会）。</p>
  <div class="action-row" style="margin-top:20px;gap:12px">
    <button type="button" class="btn-secondary" id="insufficient-dismiss-btn">知道了</button>
    <button type="button" id="watch-ad-btn">获取更多积分</button>
  </div>
  <div class="redeem-row" style="margin-top:20px">
    <input type="text" id="redeem-input" placeholder="或输入兑换码" autocomplete="off" />
    <button type="button" id="redeem-btn">使用兑换码</button>
  </div>
  <p class="field-error" id="redeem-error" style="margin-top:8px"></p>
  <p class="modal-success" id="redeem-success" style="margin-top:8px"></p>
</section>
```

- [ ] **Step 2: 新增激励广告模态框**

在 `public/index.html` 中，找到登录弹窗 `<div id="login-modal" ...>` 代码块之后、`<script>` 标签之前，插入：

```html
<!-- 激励广告弹窗 -->
<div id="reward-ad-modal" class="modal-overlay">
  <div class="modal-box">
    <h2>观看广告获取积分</h2>
    <p style="color:var(--muted);font-size:14px;margin:8px 0 16px">观看完成后可领取 10 积分（1次压缩机会）</p>

    <!-- 广告内容区域（mock：灰色占位；接入真实广告时替换此区块） -->
    <div id="ad-slot-container" style="
      width:300px;height:250px;margin:0 auto 16px;
      background:#f3f4f6;border:1px dashed #d1d5db;border-radius:12px;
      display:flex;align-items:center;justify-content:center;
      color:#9ca3af;font-size:14px;
    ">
      广告加载中...
    </div>

    <!-- 进度条 -->
    <div style="background:#e5e7eb;border-radius:4px;height:6px;margin-bottom:8px;overflow:hidden">
      <div id="ad-progress-bar" style="height:100%;width:0%;background:#374151;border-radius:4px;transition:width 1s linear"></div>
    </div>
    <p id="ad-countdown-text" style="text-align:center;font-size:13px;color:var(--muted);margin-bottom:16px">
      请观看广告，5 秒后可领取...
    </p>

    <p class="modal-error" id="ad-reward-error"></p>

    <div class="action-row">
      <button type="button" id="ad-claim-btn" disabled>请观看广告...</button>
    </div>
    <p class="modal-tip" style="margin-top:8px">每日最多可领取 10 次（100 积分）</p>
  </div>
</div>
```

- [ ] **Step 3: 验证 HTML 结构**

启动服务并打开页面：
```bash
node server-simple.js
```
在浏览器控制台运行：
```javascript
document.getElementById('insufficient-card').hidden = false;
document.getElementById('reward-ad-modal').classList.add('is-open');
```
确认：
- insufficient-card 显示正确，包含"知道了"和"获取更多积分"两个按钮
- 广告模态框弹出，显示灰色占位区域、进度条、倒计时文字、置灰的"请观看广告..."按钮

`Ctrl+C` 停止服务。

- [ ] **Step 4: Commit**

```bash
git add public/index.html
git commit -m "feat(v1.1.0): upgrade insufficient-card with 2 actions + add reward ad modal HTML"
```

---

## Task 4: 前端 JS — 广告适配器 + 完整奖励流程

**Files:**
- Modify: `public/app-simple.js` (末尾 `init()` 函数之前插入新代码；修改 `submitCompression` 中的 402 处理)

- [ ] **Step 1: 在 app-simple.js 末尾（init() 之前）插入 AdAdapter 和广告流程代码**

找到 `// ---- Init ----` 注释之前，插入以下完整代码块：

```javascript
// ---- 激励广告系统 ----

// 广告适配器（provider 由 /api/config 下发的 adProvider 决定）
const AdAdapter = {
  provider: 'mock', // 'mock' | 'adsense' | 'monetag' | 'adsterra'

  /**
   * 在 container 中加载广告内容
   * mock 实现：保持灰色占位框
   * 真实接入时：在此处插入对应 SDK 初始化代码
   */
  load(container) {
    if (this.provider === 'mock') {
      container.textContent = '广告（测试模式）';
      return Promise.resolve();
    }
    // TODO: 接入真实广告 SDK
    // if (this.provider === 'adsense') { ... }
    // if (this.provider === 'monetag') { ... }
    // if (this.provider === 'adsterra') { ... }
    return Promise.resolve();
  },

  /**
   * 开始播放/展示广告，返回 Promise
   * mock：5秒后 resolve
   * 真实接入时：监听 SDK 的 onAdRewarded/onComplete 回调后 resolve
   */
  play(onTick) {
    if (this.provider === 'mock') {
      return new Promise((resolve) => {
        let remaining = 5;
        onTick(remaining);
        const timer = setInterval(() => {
          remaining--;
          onTick(remaining);
          if (remaining <= 0) {
            clearInterval(timer);
            resolve();
          }
        }, 1000);
      });
    }
    // TODO: 真实 SDK 播放逻辑
    return Promise.resolve();
  }
};

// 激励广告弹窗元素
const rewardAdModal = document.getElementById('reward-ad-modal');
const adSlotContainer = document.getElementById('ad-slot-container');
const adProgressBar = document.getElementById('ad-progress-bar');
const adCountdownText = document.getElementById('ad-countdown-text');
const adClaimBtn = document.getElementById('ad-claim-btn');
const adRewardError = document.getElementById('ad-reward-error');

function openRewardAdModal() {
  // 重置状态
  adClaimBtn.disabled = true;
  adClaimBtn.textContent = '请观看广告...';
  adProgressBar.style.width = '0%';
  adProgressBar.style.transition = 'none';
  adCountdownText.textContent = '请观看广告，5 秒后可领取...';
  adRewardError.textContent = '';
  rewardAdModal.classList.add('is-open');

  // 加载并播放广告
  AdAdapter.load(adSlotContainer).then(() => {
    // 给浏览器一帧时间渲染，再启动进度条动画
    requestAnimationFrame(() => {
      adProgressBar.style.transition = 'width 1s linear';
      AdAdapter.play((remaining) => {
        const elapsed = 5 - remaining;
        adProgressBar.style.width = `${(elapsed / 5) * 100}%`;
        if (remaining > 0) {
          adCountdownText.textContent = `请观看广告，${remaining} 秒后可领取...`;
        } else {
          adCountdownText.textContent = '广告观看完成，点击下方按钮领取积分';
          adClaimBtn.disabled = false;
          adClaimBtn.textContent = '领取积分';
        }
      });
    });
  });
}

function closeRewardAdModal() {
  rewardAdModal.classList.remove('is-open');
}

// 关闭模态框：点击遮罩
rewardAdModal.addEventListener('click', (e) => {
  if (e.target === rewardAdModal) closeRewardAdModal();
});

// 领取积分按钮
adClaimBtn.addEventListener('click', async () => {
  adClaimBtn.disabled = true;
  adClaimBtn.textContent = '领取中...';
  adRewardError.textContent = '';

  try {
    const token = await ensureSession();
    const res = await fetch('/api/ads/reward', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({ slot_id: 'slot_reward', watch_seconds: 5 })
    });
    const data = await res.json();

    if (!res.ok) {
      adRewardError.textContent = data.message || '领取失败，请稍后重试';
      adClaimBtn.disabled = false;
      adClaimBtn.textContent = '重试';
      return;
    }

    // 更新积分显示
    updatePointsDisplay(data.new_balance);

    // 关闭广告弹窗和积分不足卡片
    closeRewardAdModal();
    document.getElementById('insufficient-card').hidden = true;

    // 自动重新触发压缩
    const file = fileInput.files?.[0];
    if (file && targetInput.value.trim()) {
      submitButton.click();
    }
  } catch (err) {
    adRewardError.textContent = '网络错误，请稍后重试';
    adClaimBtn.disabled = false;
    adClaimBtn.textContent = '重试';
  }
});
```

- [ ] **Step 2: 绑定 insufficient-card 中的两个按钮事件**

在上一步代码块之后、`// ---- Init ----` 之前，继续追加：

```javascript
// insufficient-card 按钮
document.getElementById('insufficient-dismiss-btn').addEventListener('click', () => {
  document.getElementById('insufficient-card').hidden = true;
});

document.getElementById('watch-ad-btn').addEventListener('click', () => {  // id 对应 HTML 中 id="watch-ad-btn"，按钮文案"获取更多积分"
  // 从 appConfig 中同步广告平台设置
  if (appConfig?.adProvider) {
    AdAdapter.provider = appConfig.adProvider;
  }
  openRewardAdModal();
});
```

- [ ] **Step 3: 在 fetchConfig() 中同步 adProvider**

找到 `fetchConfig()` 函数（约第92行）：

```javascript
async function fetchConfig() {
  const response = await fetch("/api/config");
  appConfig = await response.json();
  renderConfigBar();
}
```

替换为：

```javascript
async function fetchConfig() {
  const response = await fetch("/api/config");
  appConfig = await response.json();
  // 同步广告适配器平台设置
  if (appConfig?.adProvider) {
    AdAdapter.provider = appConfig.adProvider;
  }
  renderConfigBar();
}
```

- [ ] **Step 4: 验证完整流程**

启动服务：
```bash
node server-simple.js
```

打开 http://localhost:3487，执行以下手动测试：

**测试1 — 积分不足弹窗显示：**
在浏览器控制台执行：
```javascript
document.getElementById('insufficient-card').hidden = false;
```
确认卡片显示，包含"知道了"和"获取更多积分"两个按钮。

**测试2 — 知道了按钮：**
点击"知道了"按钮，确认卡片隐藏。

**测试3 — 广告弹窗和倒计时：**
在控制台执行：
```javascript
document.getElementById('insufficient-card').hidden = false;
```
点击"看广告获取积分"，确认：
- 广告模态框弹出
- 进度条在5秒内从0%到100%
- 倒计时文字每秒更新："5秒后可领取" → "4秒..." → ... → "广告观看完成..."
- "请观看广告..."按钮变为"领取积分"并可点击

**测试4 — 领取积分并自动压缩：**
上传一个PDF并填写目标大小。然后在控制台将积分清零：
```javascript
// 先查看当前积分值，然后多次压缩耗尽积分
```
当出现"积分不足"卡片时，点击"看广告获取积分"，等待5秒倒计时结束，点击"领取积分"，确认：
- 积分显示 +10
- 积分不足卡片消失
- 自动重新开始压缩

`Ctrl+C` 停止服务。

- [ ] **Step 5: Commit**

```bash
git add public/app-simple.js
git commit -m "feat(v1.1.0): add AdAdapter mock + reward ad flow + auto-retry compression"
```

---

## Task 5: 添加 btn-secondary 样式（如不存在）

**Files:**
- Modify: `public/styles.css`

- [ ] **Step 1: 检查现有样式**

```bash
grep -n "btn-secondary\|action-row button" public/styles.css | head -20
```

- [ ] **Step 2: 若无 btn-secondary 样式，追加以下 CSS**

在 `public/styles.css` 末尾追加：

```css
/* 次要按钮（用于弹窗中的"知道了"等取消操作） */
.btn-secondary {
  background: transparent;
  color: var(--fg);
  border: 1px solid var(--border, #e5e7eb);
}
.btn-secondary:hover {
  background: var(--surface-2, #f3f4f6);
}
```

- [ ] **Step 3: 验证样式**

启动服务打开页面，控制台执行：
```javascript
document.getElementById('insufficient-card').hidden = false;
```
确认"知道了"按钮为次要样式（无背景/带边框），"看广告获取积分"为主要按钮样式。

- [ ] **Step 4: Commit**

```bash
git add public/styles.css
git commit -m "feat(v1.1.0): add btn-secondary style for dismiss button"
```

---

## Task 6: 端到端回归测试 + 版本标记

**Files:**
- No code changes — verification only, then version tag

- [ ] **Step 1: 完整流程回归测试**

启动服务：
```bash
node server-simple.js
```
按顺序测试以下场景：

| 场景 | 操作 | 预期结果 |
|-----|------|---------|
| 品牌名 | 打开首页 | 标签页标题为"TinyPDF - 精准压缩 PDF" |
| 品牌名 | 查看 header | 显示"TinyPDF" |
| 正常压缩 | 上传PDF + 设置目标大小 + 点击压缩 | 压缩正常进行，积分-10 |
| 积分不足 | 耗尽积分后点击压缩 | 出现"积分不足"卡片，含两个按钮 |
| 知道了 | 点击"知道了" | 卡片消失，回到上传状态 |
| 看广告 | 点击"看广告获取积分" | 广告模态框弹出 |
| 倒计时 | 等待5秒 | 进度条填满，按钮变为"领取积分" |
| 领取积分 | 点击"领取积分" | 积分+10，弹窗关闭，自动开始压缩 |
| 每日上限 | 领取10次后再次尝试 | 提示"今日领取次数已达上限（10次）" |
| config接口 | 访问 /api/config | 返回含 `adProvider: "mock"` 字段 |

- [ ] **Step 2: 创建 v1.1.0 版本标签**

```bash
git tag -a v1.1.0 -m "TinyPDF v1.1.0: brand rename + rewarded ad flow with mock adapter"
```

- [ ] **Step 3: 验证 tag**

```bash
git log --oneline -6
git tag | grep v1
```
预期看到 `v1.1.0` 标签及本次所有提交。

---

## 自检结果

**Spec 覆盖核查：**
- [x] 网站名改为 TinyPDF → Task 1
- [x] 积分不足后弹窗 → Task 3 (HTML) + Task 4 (JS 中402处理已存在，insufficient-card 显示逻辑不变)
- [x] 弹窗左"知道了"、右"看广告获取" → Task 3 Step 1
- [x] 点击获取积分播放广告 → Task 4 Step 1 (openRewardAdModal)
- [x] 5秒倒计时 mock → Task 4 Step 1 (AdAdapter.play)
- [x] 预留 AdSense/Monetag/Adsterra 接口 → Task 4 Step 1 (AdAdapter.provider + TODO 注释)
- [x] 完播后 +10 积分 → Task 2 (/api/ads/reward) + Task 4 (adClaimBtn handler)
- [x] 积分更新后自动重压 → Task 4 Step 1 (submitButton.click())
- [x] 每日最多10次防刷 → Task 2 Step 2

**类型一致性：**
- `AdAdapter.play(onTick)` 在定义和调用处签名一致 ✓
- `/api/ads/reward` 请求体字段 `slot_id`/`watch_seconds` 与后端解析一致 ✓
- 响应字段 `new_balance` 与前端 `updatePointsDisplay(data.new_balance)` 一致 ✓

**无占位符** ✓
